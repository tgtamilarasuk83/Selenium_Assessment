package assessment;


import java.time.Duration;
import java.util.ArrayList;
import java.util.Collections;
import java.util.List;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.edge.EdgeDriver;
import org.openqa.selenium.interactions.Actions;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;
public class demoblaze {
	public static void main(String args[]) {
		System.out.println("hello");
		 
		WebDriver driver=new EdgeDriver();
		
		
		// Launch the browser using Selenium WebDriver. 
		driver.get("https://demoblaze.com/");
	
		//implicit wait
		driver.manage().timeouts().implicitlyWait(Duration.ofSeconds(30));
		
		
		//"Login Successful
		driver.findElement(By.xpath("//a[@id='login2']")).click();
		
		driver.findElement(By.xpath("//input[@id='loginusername']")).sendKeys("arasu");
		driver.findElement(By.xpath("(//input[@id='loginpassword'])[1]")).sendKeys("arasu");
		driver.findElement(By.xpath("//button[normalize-space()='Log in']")).click();
			
		
//		//Validation
//		String Element = driver.findElement(By.xpath("//a[contains(text(), 'Welcome arasu')]")).getText();
//		String Element2 ="Welcome arasu";
//		if(Element.equals(Element2)) {
//			System.out.println("\"Login Successful");
//		}
//		
		
		
		
		//Category Navigation & Product Handling (10 Marks) 
		//use explicit wait for the element
		
		
//    WebDriverWait wait = new WebDriverWait(driver, Duration.ofSeconds(15));
//	
//    WebElement laptop = wait.until(
//            ExpectedConditions.elementToBeClickable(By.xpath("//a[text()='Laptops']")));
//    
//    laptop.click();
		
		
		
		Actions act = new Actions(driver);
		
	
		WebElement laptop = driver.findElement(By.xpath("//a[text()='Laptops']"));
		//laptop.click();
		act.moveToElement(laptop).click().perform();
		        
		ArrayList<WebElement> list = new ArrayList<WebElement>();
		
		 list =  (ArrayList<WebElement>) driver.findElements(By.xpath("//a[@class=\"hrefch\"]"));
		 
		 ArrayList<String> list1 = new ArrayList<String>();
		 
		 for(WebElement E : list) {
			 list1.add(E.getText());
		 }
		
		 Collections.sort(list1);
		 
		
		for(String X : list1) {
			System.out.println("Found Laptop:"+X);
		}
		 
			
	
		//Add Product to Cart
		
		 driver.findElement(By.xpath("//a[text()='Laptops']"));
		 driver.findElement(By.xpath("//a[text()='Laptops']"));
		 driver.findElement(By.xpath("//a[text()='Laptops']"));
		 
		 driver.findElement(By.xpath("//a[normalize-space()='MacBook Pro']")).click();
		 driver.findElement(By.xpath("//a[@class='btn btn-success btn-lg']")).click();
		 
		
		
		
		

	}

}
